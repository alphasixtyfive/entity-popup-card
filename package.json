{
  "name": "entity-popup-card",
  "version": "1.3.1",
  "private": true,
  "type": "module",
  "description": "A compact live entity popup for Home Assistant dashboards",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check-build": "node scripts/build.mjs --check",
    "test": "node --test entity-popup-card.test.mjs && npm run check-build",
    "format": "prettier --write src scripts entity-popup-card.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "prettier": "^3.6.0"
  }
}
